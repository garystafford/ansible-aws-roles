version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - pip install --upgrade -r requirements.txt -q
  build:
    commands:
      - ansible-playbook --inventory inventories/aws_ec2.yml playbooks/10_cfn_network.yml --tags create -vvv
